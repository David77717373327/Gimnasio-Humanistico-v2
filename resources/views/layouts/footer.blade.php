<!-- ============================================
     FOOTER INSTITUCIONAL OPTIMIZADO PARA SEO
============================================ -->

<footer class="main-footer" role="contentinfo" aria-label="Información institucional del Colegio Gimnasio Humanístico">

    <!-- Línea decorativa -->
    <div class="footer-decoration" aria-hidden="true"></div>

    <div class="footer-top">
        <div class="container">
            <div class="row g-4">

                <!-- Información de la Institución -->
                <div class="col-lg-3 col-md-6">
                    <div class="footer-section footer-brand" data-aos="fade-up" data-aos-delay="100">

                        <a href="{{ url('/') }}" class="footer-logo" aria-label="Volver al inicio del Colegio Gimnasio Humanístico">
                            <div class="footer-logo-icon">
                                <i class="fas fa-graduation-cap" aria-hidden="true"></i>
                            </div>
                            <span class="titulo-footer-colegio">Gimnasio Humanístico</span>
                        </a>

                        <p class="footer-description">
                            Colegio de formación integral con excelencia académica, valores cristianos y compromiso social por más de 25 años.
                        </p>

                        <!-- Redes sociales con SEO -->
                        <div class="social-links" aria-label="Redes sociales">
                            <a href="https://web.facebook.com/profile.php?id=100041995064524" class="social-link facebook" aria-label="Facebook del Colegio">
                                <i class="fab fa-facebook-f" aria-hidden="true"></i>
                            </a>
                            <a href="https://www.instagram.com/gimnasiohumanistico03?utm_source=qr&igsh=YTV3aGt3ajVyZjRw" class="social-link instagram" aria-label="Instagram del Colegio">
                                <i class="fab fa-instagram" aria-hidden="true"></i>
                            </a>

                            <!-- Nueva red social TikTok -->
                            
                            <a href="https://www.tiktok.com/@humanisticoneiva" class="social-link tiktok" aria-label="TikTok del Colegio">
                                <i class="fab fa-tiktok" aria-hidden="true"></i>
                            </a>

                            <a href="https://www.youtube.com/@silviacristinaruiz2435" class="social-link youtube" aria-label="YouTube del Colegio">
                                <i class="fab fa-youtube" aria-hidden="true"></i>
                            </a>
                            <a href="https://wa.me/573152296832" class="social-link whatsapp" aria-label="WhatsApp del Colegio">
                                <i class="fab fa-whatsapp" aria-hidden="true"></i>
                            </a>

                            



                        </div>
                    </div>
                </div>

                <!-- Enlaces Rápidos -->
                <div class="col-lg-2 col-md-6">
                    <div class="footer-section" data-aos="fade-up" data-aos-delay="200">

                        <h5 class="footer-title">Enlaces Rápidos</h5>

                        <ul class="footer-links" role="list">
                            <li><a href="{{ url('/') }}" class="footer-link" aria-label="Ir a la sección Inicio"><i class="fas fa-chevron-right" aria-hidden="true"></i> Inicio</a></li>
                            <li><a href="{{ route('historia') }}" class="footer-link" aria-label="Ir a la sección Quiénes Somos"><i class="fas fa-chevron-right" aria-hidden="true"></i> Quiénes Somos</a></li>
                            <li><a href="{{ route('preescolar') }}" class="footer-link" aria-label="Ir a Oferta Académica"><i class="fas fa-chevron-right" aria-hidden="true"></i> Oferta Académica</a></li>
                            <li><a href="{{ route('admision') }}" class="footer-link" aria-label="Ir a Admisiones"><i class="fas fa-chevron-right" aria-hidden="true"></i> Admisiones</a></li>
                        </ul>

                    </div>
                </div>

                <!-- Servicios -->
                <div class="col-lg-2 col-md-6">
                    <div class="footer-section" data-aos="fade-up" data-aos-delay="300">

                        <h5 class="footer-title">Servicios</h5>

                        <ul class="footer-links" role="list">
                            <li><a href="{{ route('preescolar') }}" class="footer-link" aria-label="Educación Preescolar"><i class="fas fa-chevron-right" aria-hidden="true"></i> Educación Preescolar</a></li>
                            <li><a href="{{ route('basica.primaria') }}" class="footer-link" aria-label="Básica Primaria"><i class="fas fa-chevron-right" aria-hidden="true"></i> Básica Primaria</a></li>
                            <li><a href="{{ route('basica.secundaria') }}" class="footer-link" aria-label="Básica Secundaria"><i class="fas fa-chevron-right" aria-hidden="true"></i> Básica Secundaria</a></li>
                            <li><a href="{{ route('media.academica') }}" class="footer-link" aria-label="Media Académica"><i class="fas fa-chevron-right" aria-hidden="true"></i> Media Académica</a></li>
                        </ul>
                    </div>
                </div>

                <!-- Contacto -->
                <div class="col-lg-2 col-md-6">
                    <div class="footer-section" data-aos="fade-up" data-aos-delay="400">

                        <h5 class="footer-title">Contacto</h5>

                        <!-- Dirección -->
                        <div class="contact-item">
                            <div class="contact-icon">
                                <i class="fas fa-map-marker-alt" aria-hidden="true"></i>
                            </div>
                            <div class="contact-content">
                                <div class="contact-label">Dirección</div>
                                <p class="contact-text">CLL.13 4-53<br>Neiva, Huila - Colombia</p>
                            </div>
                        </div>

                        <!-- Teléfonos -->
                        <div class="contact-item">
                            <div class="contact-icon">
                                <i class="fas fa-phone" aria-hidden="true"></i>
                            </div>
                            <div class="contact-content">
                                <div class="contact-label">Teléfono</div>
                                <p class="contact-text">(+57) 8 875 2345<br>(+57) 300 123 4567</p>
                            </div>
                        </div>

                        <!-- Email -->
                        <div class="contact-item">
                            <div class="contact-icon">
                                <i class="fas fa-envelope" aria-hidden="true"></i>
                            </div>
                            <div class="contact-content">
                                <div class="contact-label">Email</div>
                                <p class="contact-text">gimnasiohumanistico@hotmail.com</p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Ubicación con mapa -->
                <div class="col-lg-3 col-md-12">
                    <div class="footer-section" data-aos="fade-up" data-aos-delay="500">

                        <h5 class="footer-title">Nuestra Ubicación</h5>

                        <div class="footer-map" aria-label="Mapa de ubicación del colegio">
                            <iframe 
                                title="Mapa del Colegio Gimnasio Humanístico"
                                src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3984.5980542106286!2d-75.29349652622278!3d2.9312627970450427!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x8e3b74649b7ec0f3%3A0x6bd869e2c80a8862!2sGimnasio%20Human%C3%ADstico%20del%20Alto%20Magdalena!5e0!3m2!1ses!2sco!4v1758286578845!5m2!1ses!2sco"
                                width="600"
                                height="450"
                                style="border:0;"
                                allowfullscreen=""
                                loading="lazy"
                                referrerpolicy="no-referrer-when-downgrade">
                            </iframe>
                        </div>

                    </div>
                </div>

            </div>
        </div>
    </div>

    <!-- Parte inferior del footer -->
    <div class="footer-bottom">
        <div class="container">
            <div class="footer-bottom-content" data-aos="fade-up" data-aos-delay="600">

                <p class="copyright">
                    © 2024 Gimnasio Humanístico. Todos los derechos reservados.
                </p>

                <div class="footer-bottom-links" aria-label="Enlaces legales">
                    <a href="#" class="footer-bottom-link">Política de Privacidad</a>
                    <a href="#" class="footer-bottom-link">Términos y Condiciones</a>
                    <a href="#" class="footer-bottom-link">Manual de Convivencia</a>
                </div>
            </div>
        </div>
    </div>
</footer>


     <!-- Script para Animaciones AOS (Agregar antes del cierre de </body>) -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" rel="stylesheet">
    <script>
        // Inicializar AOS
        AOS.init({
            duration: 500,
            easing: 'ease-out',
            once: true,
            offset: 50
        });

        // JavaScript mejorado para el footer
        document.addEventListener('DOMContentLoaded', function() {

            // Observer para efectos adicionales
            const footerObserver = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {

                        // Animación escalonada para los enlaces
                        const footerLinks = entry.target.querySelectorAll('.footer-link');
                        footerLinks.forEach((link, index) => {
                            setTimeout(() => {
                                link.style.opacity = '1';
                                link.style.transform = 'translateX(0)';
                            }, index * 100);
                        });

                        // Efecto para los iconos sociales
                        const socialLinks = entry.target.querySelectorAll('.social-link');
                        socialLinks.forEach((social, index) => {
                            setTimeout(() => {
                                social.style.opacity = '1';
                                social.style.transform = 'scale(1)';
                            }, 300 + (index * 100));
                        });

                        // Animación especial para el footer-bottom
                        const footerBottom = entry.target.querySelector('.footer-bottom-content');
                        if (footerBottom) {
                            setTimeout(() => {
                                footerBottom.style.opacity = '1';
                                footerBottom.style.transform = 'translateY(0)';
                            }, 500);
                        }

                        footerObserver.unobserve(entry.target);
                    }
                });
            }, {
                threshold: 0.2
            });

            // Observar el footer
            const footer = document.querySelector('.main-footer');
            if (footer) {
                footerObserver.observe(footer);
            }

            // Inicializar estilos de los elementos animados
            document.querySelectorAll('.footer-link').forEach(link => {
                link.style.opacity = '0';
                link.style.transform = 'translateX(-20px)';
                link.style.transition = 'all 0.3s ease-out';
            });

            document.querySelectorAll('.social-link').forEach(social => {
                social.style.opacity = '0';
                social.style.transform = 'scale(0.8)';
                social.style.transition = 'all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275)';
            });

            // Inicializar footer-bottom-content
            const footerBottomContent = document.querySelector('.footer-bottom-content');
            if (footerBottomContent) {
                footerBottomContent.style.opacity = '0';
                footerBottomContent.style.transform = 'translateY(30px)';
                footerBottomContent.style.transition = 'all 0.6s ease-out';
            }

            // Efectos hover adicionales
            document.querySelectorAll('.footer-link').forEach(link => {
                link.addEventListener('mouseenter', function() {
                    this.style.textShadow = '0 0 10px rgba(244, 185, 66, 0.5)';
                });

                link.addEventListener('mouseleave', function() {
                    this.style.textShadow = 'none';
                });
            });

            // Parallax sutil para la decoración
            let ticking = false;

            function updateParallax() {
                const scrolled = window.pageYOffset;
                const decoration = document.querySelector('.footer-decoration');
                if (decoration) {
                    const speed = scrolled * 0.1;
                    decoration.style.transform = `translateY(${speed}px) rotate(${scrolled * 0.05}deg)`;
                }
                ticking = false;
            }

            window.addEventListener('scroll', function() {
                if (!ticking) {
                    requestAnimationFrame(updateParallax);
                    ticking = true;
                }
            });
        });

        // Animación adicional para contadores
        function animateWelcomeCounters() {
            const counters = document.querySelectorAll('.stat-mini-number');
            counters.forEach(counter => {
                const target = counter.textContent;
                const numericTarget = parseInt(target.replace(/\D/g, ''));
                const suffix = target.replace(/\d/g, '');
                let current = 0;
                const increment = numericTarget / 50;
                const timer = setInterval(() => {
                    current += increment;
                    if (current >= numericTarget) {
                        current = numericTarget;
                        clearInterval(timer);
                    }
                    counter.textContent = Math.floor(current) + suffix;
                }, 40);
            });
        }

        // Observador para activar animaciones de contadores
        const welcomeObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting && entry.target.classList.contains('welcome-stats-mini')) {
                    animateWelcomeCounters();
                    welcomeObserver.unobserve(entry.target);
                }
            });
        }, {
            threshold: 0.5
        });

        // Observar la sección de estadísticas
        document.addEventListener('DOMContentLoaded', function() {
            const statsSection = document.querySelector('.welcome-stats-mini');
            if (statsSection) {
                welcomeObserver.observe(statsSection);
            }
        });
    </script>
